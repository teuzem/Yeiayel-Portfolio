{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "corepack enable && corepack prepare pnpm@11.25.0 --activate && pnpm build",
  "installCommand": "corepack enable && corepack prepare pnpm@11.25.0 --activate && pnpm install --frozen-lockfile"
}
